export default class Reserva {
  constructor(reserva_id,	cliente_id,	mesa_id, fecha_reserva,	estado) {
    this.reserva_id = null;
    this.cliente_id = null;
    this. mesa_id= null;
    this.fecha = fecha;
    this.estado = true // Estado por defecto al crear la reserva
}
}